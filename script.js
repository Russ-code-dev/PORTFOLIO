
// The code to select all the nav-links
const navlist = document.querySelector('.nav-link');

nav-links.forEach(link=>{
    link.addEventListenner('click', fuction(event)){
        event.preventDefault();  //stop instantjump
    }

    Get section id from link (remove the )
})